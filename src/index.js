/**
 * @format
 */
import {AppRegistry} from 'react-native';
import App from './App';

AppRegistry.registerComponent('practice', () => App);

AppRegistry.runApplication('practice', {
  rootTag: document.getElementById('root'),
});
